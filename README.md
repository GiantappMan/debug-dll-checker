# debug-dll-checker
a debug dll checker for dotnet

# How to use
```
# install 
dotnet tool install --global DDC

# upgarde
dotnet tool update --global DDC
```
```
# run in commandline
ddc

# or 
ddc -p .\debug\
```
